apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  labels:
    app: app-izaias
  name: podmonitor-app-izaias
  namespace: aluno-izaias
spec:
  podMetricsEndpoints:
  - port: 5000
    interval: 10s
  selector:
    matchLabels:
      app: app-izaias
